---
# defaults file for glusterfs-client-ansible-role

glusterfs_version: "3.10"

glusterfs_volumes: []

glusterfs_auto_remount: True

glusterfs_client_packages_EL:
  - {package: "glusterfs"}
  - {package: "glusterfs-fuse"}
